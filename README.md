# House Price Prediction using Linear Regression

## Project Overview

This project implements a House Price Prediction System using the Linear Regression machine learning algorithm.

The model uses different house-related features to predict the price of a house.

## Objective

The objective of this experiment is to:

- Load and explore the house price dataset.
- Select relevant input features.
- Split the dataset into training and testing data.
- Train a Linear Regression model.
- Evaluate the model using R² Score and Mean Squared Error.
- Predict the price of a new house.

## Dataset

The dataset contains 2,000 house records.

The features used for prediction are:

- `Area`
- `Bedrooms`
- `Bathrooms`
- `Floors`
- `YearBuilt`

Target variable:

- `Price`

## Technologies Used

- Python
- Pandas
- Scikit-learn
- VS Code
- Git
- GitHub

## Machine Learning Algorithm

### Linear Regression

Linear Regression is a supervised machine learning algorithm used to predict a continuous numerical value.

In this project, the model predicts the price of a house based on its features.

## Train-Test Split

The dataset is divided into:

- 80% training data
- 20% testing data

The model is trained using the training data and evaluated using the testing data.

## Model Evaluation

The Linear Regression model achieved the following results:

**R² Score:**

```text
-0.01273663755401877